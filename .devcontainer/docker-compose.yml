services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/karray:cached
    command: sleep infinity
    environment:
      - PYTHONPATH=/workspaces/karray
